/**
 * Audio index contract shared by Studio build output and the editor model.
 * Index 0 is reserved for stop/no audio by the HMI interface.
 */
export const AUDIO_INDEX_NONE = 0;
export const AUDIO_INDEX_MIN = 1;
export const AUDIO_INDEX_MAX = 100;
